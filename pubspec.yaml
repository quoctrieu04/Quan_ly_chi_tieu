name: chitieu
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+2

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  dio: ^5.4.0
  flutter_secure_storage: ^9.0.0
  provider: ^6.0.5
  shared_preferences: ^2.2.2
  fl_chart: ^0.66.0
  speech_to_text: ^7.3.0
  diacritic: ^0.1.6  
  flutter_tts: ^3.8.3
  
  # i18n
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2

  cupertino_icons: ^1.0.8
  flutter_gen: ^5.11.0
  change_app_package_name: ^1.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  generate: true

  assets:
    - assets/voice_keywords.json
    - assets/default_voice_synonyms.json
